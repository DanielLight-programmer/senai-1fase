programa {
  funcao inicio() { 
    //Telles Transportes
    
    //caso 1
    inteiro frete = 15
    inteiro peso = 20
    inteiro distancia = 650 
    inteiro volume = 1
    escreva( frete + (peso*2) + (0.05*distancia) + (10*volume) )

    //caso2
      frete = 15
      peso= 60
      distancia = 1000 
      volume = 0.5
    escreva( frete + (peso*2) + (0.05*distancia) + (10*volume) )






  }
}
